<div class="container mt-3">
  <div class="row">
    <div class="col-12">
      <h1>All our Shops</h1>
    </div>
  </div>

  <div class="row">
    <div class="col">
      <% if @query %>
        <p>You searched for <strong><%= @query %></strong>. We found <%= @shops.count %> shops out of our <%= Shop.count %> on our website.</p>
      <% else %>
        <p>All <%= Shop.count %> shops.</p>
      <% end %>
    </div>
  </div>

</div>  