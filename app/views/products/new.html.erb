<div class="container">
  <%= simple_form_for([@shop, @product]) do |f| %>
    <%= f.input :title %>
    <%= f.input :description %>
    <%= f.input :amount %>
    <%= f.input :price %>
    <%= f.input :image, as: :file %>
    <%= f.submit "Subir producto", class:"btn btn-add" %>
  <% end %>
</div>